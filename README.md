# R2.12

# Install
Clone the repository
In a virtual environnment or anywhere I don't care it's not my computer execute: pip install -r /path/to/requirements.txt
-> Will download the requirements

# DO NOT commit on main, use branches
# DO NOT edit SQLite DB or python code UNLESS you know what you're doing
Using Github Desktop is advised as it makes git pull/push process easier